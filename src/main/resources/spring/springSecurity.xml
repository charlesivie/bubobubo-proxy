<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                    http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                    http://www.springframework.org/schema/security
                    http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <http use-expressions="true" authentication-manager-ref="authenticationManager" create-session="stateless">
        <intercept-url pattern="/repositories/**" access="hasRole(request.getAttribute('repositoryId'))"/>
        <http-basic entry-point-ref="restAuthenticationEntryPoint" />
    </http>

    <beans:bean id="loggerListener" class="org.springframework.security.authentication.event.LoggerListener"/>

    <!-- Configure Authentication mechanism -->
    <authentication-manager alias="authenticationManager" id="authenticationManager">
        <authentication-provider>
            <password-encoder ref="twoWayPasswordEncoder" />
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query="
                               SELECT U.username_da AS username,
                               U.password_da as password,
                               U.enabled_da as enabled
                               FROM SecureUser U where U.username_da=?"
                               authorities-by-username-query="
                               SELECT U.username_da as username,
                               A.name_da as authority
                               FROM SecureUser U
                               left join SecureUser_SecureRole UA on U.id=UA.SecureUser_id
                               left join SecureRole A on UA.roles_id = A.id
                               WHERE U.username_da=?" />
        </authentication-provider>
    </authentication-manager>

</beans:beans>